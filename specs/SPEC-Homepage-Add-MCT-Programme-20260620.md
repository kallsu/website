# Feature: Add MCT Programme Navigation Link

## Requirements

- Add a `MCT Programme` link to the main navigation in `src/index.html`
- Point the new navigation link to `src/mct-programme.html`
- Update `src/index.md` so it reflects the new navigation entry
- Preserve the existing homepage content and all other navigation links

## Business Rules

- The website must remain static and use only HTML5 and CSS3
- The new link must use the existing navigation pattern and styling
- Markdown content must stay aligned with the HTML homepage source

## Acceptance Criteria

- The homepage navigation includes a visible `MCT Programme` link
- The link target resolves to `src/mct-programme.html`
- `src/index.md` documents the same navigation entry
- No unrelated homepage content changes are introduced

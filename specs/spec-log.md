# Plan

1. Create a new spec for removing the Activities navigation item and homepage section.
2. Update `src/index.html` to remove the Activities link and the Activities body section.
3. Update `src/index.md` so it matches the homepage content after the removal.
4. Review the changed files for consistency with the repo rules.

# Plan

1. Create a new spec for adding the `MCT Programme` navigation link.
2. Update `src/index.html` to add the `MCT Programme` navbar entry pointing to `src/mct-programme.html`.
3. Update `src/index.md` so it reflects the new navigation entry.
4. Review the changed files for consistency with the repo rules.
